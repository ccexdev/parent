post:
  tags:
    - Authentication
  operationId: token
  summary: Generate jwt
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../schemas/LoginUser.yml'
  responses:
    200:
      description: User login successful
      content:
        application/json:
          schema:
            $ref: "../schemas/TokenResponse.yml"
